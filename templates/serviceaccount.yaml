{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "3xui.serviceAccountName" . }}
  labels:
    {{- include "3xui.labels" . | nindent 4 }}
{{- end }}